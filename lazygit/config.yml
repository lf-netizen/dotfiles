# Keep LazyGit's default layout and keybindings.
gui:
  # Fixed line count: approximate half-page scrolling for Ctrl+U / Ctrl+D.
  # Also applies to PgUp/PgDn, Shift+J/K, and mouse-wheel scrolling.
  scrollHeight: 15

git:
  diffRenderers:
    - name: delta
      command: >-
        delta --dark --paging=never --true-color=always
        --syntax-theme=kanagawa --line-numbers
        --hunk-header-style="line-number syntax"
        --minus-style="syntax #49282e" --minus-emph-style="syntax #753235"
        --plus-style="syntax #243d2f" --plus-emph-style="syntax #2b5c37"
